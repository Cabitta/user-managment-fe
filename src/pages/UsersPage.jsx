/**
 * UsersPage.jsx — Panel de administración.
 *
 * Responsabilidad: Listar usuarios con búsqueda y paginación.
 * Accesible solo para el rol 'admin' (spec 4.2).
 */
export default function UsersPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Administración de Usuarios</h1>
      <p className="text-muted-foreground">Lista de usuarios (Placeholder)</p>
    </div>
  );
}
